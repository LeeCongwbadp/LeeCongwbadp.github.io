## HTML学习总结
### HTML概述
- `HTML` 是超文本标记语言（ `HyperText Markup Language` ）的缩写。我们用 `HTML` 来构建 `Web` 页面即所谓的网页。
- `HTML` 以外的其它技术则通常用来描述一个网页的表现与展示效果（如 `CSS` ）或功能与行为（如 `JavaScript`），人们常把 `HTML` 、 `CSS` 、 `JavaScript` 统称为“**Web程序三剑客**”。
- HTML 不是一门编程语言，而是一种用于定义内容结构的**标记语言**。



